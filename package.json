{
  "name": "design-md-kit",
  "version": "0.0.0",
  "private": true,
  "description": "The execution layer for DESIGN.md — parser, CLI, validator, exporters. By Glamgar.",
  "repository": "GlamgarOnDiscord/uxui-AI-Prompt",
  "license": "MIT",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rimraf dist node_modules && rimraf node_modules"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
